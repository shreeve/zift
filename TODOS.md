# Zift TODOs

Living gap list between the implementation and PLAN.md. Reviewed jointly by the in-Cursor agent (Claude Opus) and the peer agent (GPT-5.5) on 2026-04-26 against the current `main` branch.

Conventions:

- One bullet = one actionable task. Tick the box when it lands.
- When a bullet completes, move it (preserving its `[x]` state and citation) to `## DONE` at the top.
- Keep PLAN.md citations on every bullet so we can re-derive intent if the issue resurfaces.
- Severity: **P0** = production blocker, **P1** = major spec gap, **P2** = polish/ergonomics, **P3** = nit/bikeshed.
- The "Disputed" section lists items the peer review raised that we deliberately do not accept; they live there for the record.

---

## DONE

- [x] Build scaffold: Zig 0.16, libssh 0.12 via translateC, build_options module, `.gitignore`.
- [x] Config DSL parser: indentation-based grammar, `server` and `user` sections, `allow`/`deny` rules, `password` PHC validation, `key` ed25519/ECDSA validation, line-oriented errors at the file level (PLAN §6.2, §6.3).
- [x] Argon2id parameter envelope enforcement (m: 64–256 MiB, t: 2–8, p: 1–4) with rejection at parse time (PLAN §8.4).
- [x] Argon2id dummy-hash path for unknown users using upper-bound parameters (PLAN §8.4).
- [x] Path jail: `normalizeVirtualPath`, `joinRoot`, `isInsideRoot`, `verifyFile`/`verifyDir` post-open FD verification, `openVerifiedParent` for TOCTOU-safe mutations (PLAN §8.3).
- [x] Policy engine with literal-prefix and `*`/`?` matchers respecting `/` boundaries (PLAN §6.3).
- [x] SFTP v3 raw framing over libssh: `INIT` → `VERSION`, `REALPATH`, `STAT`/`LSTAT`, `OPENDIR`/`READDIR`, `OPEN`/`READ`/`WRITE`/`CLOSE`, `MKDIR`/`REMOVE`/`RMDIR`/`RENAME`.
- [x] `REALPATH` echoes the client's normalized virtual path (PLAN §7.6).
- [x] Public-key authentication, two-phase libssh flow, ed25519 + ECDSA only, RSA/DSA rejected at config load (PLAN §8.4).
- [x] mtime-based config reload with refcounted immutable snapshots, sessions hold their own snapshot for life (PLAN §7.3).
- [x] Thread-per-connection with detached worker threads (PLAN §7.5).
- [x] Audit JSON to stderr: stable field order, JSON-string escaping for path/detail (PLAN §8.5).
- [x] CLI: `zift serve <config>`, `zift validate <config>`, `zift hash-password`, `zift version` (PLAN §4.8).
- [x] Operational signal cluster: `SIGTERM`/`SIGINT` → graceful drain with 30s grace cap, `SIGHUP` → forced reload regardless of mtime, `SIGUSR1` → flag (consumer pending), `SIGPIPE` → ignored (PLAN §7.2).
- [x] `idle-timeout` enforced at the SFTP read loop with `idle.timeout` audit line (PLAN §6.2).
- [x] `max-connections` enforced post-accept with `accept` denial audit line (PLAN §6.2).
- [x] Accept loop driven by `poll(bind_fd)` so signals are visible within ~1 second.
- [x] **Integration test harness** (`test/`): bash runner, paramiko venv, per-test scratch dir + unique TCP port, `bg`/`wait_bg` helpers that don't conflict with the backgrounded zift, baseline coverage for smoke/idle-timeout/max-connections/SIGHUP/SIGTERM. Each case starts with `# Test: …` so `test/run.sh --list` is self-documenting.
- [x] **Per-handle access mode tracking** (`session.zig` `Handle.can_read`/`can_write`). OPEN parses the SFTP flag bitmask precisely (READ/WRITE/APPEND/CREAT/TRUNC), gates BOTH `.open_read` and `.open_write` policies if both bits are requested, and stamps the resulting access on the handle. `handleRead`/`handleWrite` now return `SSH_FX_PERMISSION_DENIED` with `read|write result=denied` audit lines when the handle lacks the bit. PLAN §6.3. Regression: `test/cases/10-handle-access-mode.sh` (paramiko probe).
- [x] **Symlink-escape closed in `SSH_FXP_OPEN`** (`session.zig` `handleOpen`). Replaced the path-string `createFile`-then-verify pattern with FD-relative open via `openVerifiedParent` plus `follow_symlinks = false` on the basename, post-open `verifyFile` belt-and-suspenders, and deferred truncation (`file.setLength(0)` runs *only* after verification). `SSH_FXF_CREAT` uses `exclusive=true` for race-free creation; `SSH_FXF_EXCL` and `SSH_FXF_TRUNC` honored explicitly. A symlink at the final component now returns `SSH_FX_PERMISSION_DENIED` and never modifies the target. PLAN §8.3. Regression: `test/cases/11-symlink-escape.sh` (oracle = byte-exact comparison of an outside-jail file before vs after the attack).
- [x] **Operational polish: stderr for status, accept.rejected, drop listen socket on drain, scriptable hash-password, matched-key audit** (`main.zig` + `session.zig`). Five small operator-facing fixes batched into one commit. (a) PLAN §7.4 says human-readable status goes to stderr; stdout is reserved for things scripts genuinely consume (`hash-password` PHC output, `version`). The `serve` startup banners and `session.run`'s "listening on" line now go to stderr. (b) `max-connections` denial audit operation renamed from `accept` to `accept.rejected` so `jq`/awk consumers get a stable, unambiguous filter. (c) On SIGTERM drain start, the listening fd is now `close()`-d immediately so no fresh TCP connections land during the grace window — `defer ssh_bind_free` still tears down the libssh state on function return; this just unbinds the port promptly. PLAN §7.1. (d) `zift hash-password` no longer writes a `password: ` prompt to stderr — it's now a pure stdin→stdout filter, cleanly pipelineable (`printf '%s\n' "$pw" | zift hash-password`). PLAN §11.3. (e) `matchesAnyConfiguredKey` returns the matched index (`?usize`) instead of `bool`; the successful pubkey audit now logs `user.keys[matched_idx].algorithm` instead of always `keys[0].algorithm`, so users with multiple configured keys see the actual key that authenticated.
- [x] **Reload semantics: interval cadence, forward-only mtime, stat-failure warning, host-key check on reload** (`session.zig` accept loop + `ActiveConfig.reloadIfChanged`). Closes four P1 bullets in one commit. (a) The accept loop now drives `reloadIfChanged` from a monotonic deadline computed from `cfg.server.reload_interval_ms`, not from every poll-timeout. `reload-interval 0` disables mtime polling entirely (SIGHUP still works). PLAN §7.3. (b) Reload only triggers when mtime moves *forward*: the `==` check became `<=`, so atomic-deploy patterns that rewind or preserve mtime no longer surprise the running server. SIGHUP's `forceReload` path is unaffected. (c) `currentConfigMtime` failures are now warned exactly once on transition into the bad state (`zift: cannot stat config file: ...: FileNotFound (keeping previous config)`) and recovery is announced once on transition back (`zift: config file readable again`). The `ActiveConfig.stat_warned` flag tracks the state; subsequent failures while already-warned are silent. (d) Host-key readability check on reload was already in `config.validateSemantic` (which `applyReload` calls); the test now proves it: a reload that swaps in an unreadable host-key path is rejected with `host-key unreadable` and the previous config keeps serving — verified by an SFTP auth that succeeds against the still-running old listener. Regression: `test/cases/23-reload-semantics.sh` (six sub-scenarios — forward-mtime triggers reload, rewound-mtime does NOT, stat-failure warning, recovery message, reload-interval=0 disables polling but SIGHUP still works, reload rejects bad host-key + previous keeps serving).
- [x] **Config grammar: line-numbered structured errors, inline-comment rejection, suffix-required durations** (`config.zig` `ParseDiag` + `parseWithDiag` + `parseDurationMs` rewrite + comment-handling rewrite). Closes four P1 bullets in one commit. (a) New `parseWithDiag(gpa, text, diag)` populates a `ParseDiag` struct via `errdefer` on any error path: `line`, `section_kind`, `user_name`, `key`. Strings are stored in inline fixed buffers (sized at PLAN §7.6 limits) so the diag survives the arena's `deinit` that runs on the same error path — slices into the arena would dangle by the time the caller reads them. (b) `ParseDiag.format` produces `line N: [user X] 'key': ErrorName` so callers (zift validate / serve startup / runtime reload) emit `zift: <file>:line N: [user X] 'key': ErrorName` instead of just `@errorName(err)`. (c) Inline `#` comments are now rejected with `error.InlineComment` — only whole-line comments (first non-whitespace char is `#`) are recognized. PLAN §6.2 grammar. (d) Bare-number durations are now rejected with `error.InvalidDuration`; the suffixes `ms`/`s`/`m`/`h` are required. Bare `0` is special-cased as the documented disabled sentinel for `idle-timeout`/`reload-interval` so operators don't have to write `0s`. Regression: `test/cases/22-config-grammar.sh` (six sub-scenarios — line-numbered unknown-key, inline-comment rejection, whole-line-comment acceptance, bare-number rejection, all-four-suffix acceptance, bare-`0` acceptance).
- [x] **Auth hardening: cached dummy hash, pubkey timing parity, attempt cap** (`auth.zig` `ensureDummyHash` + `session.zig` `matchAgainstDummyKey` + `failed_attempts` ceiling). Three security wins in one commit. (a) `auth.ensureDummyHash` lazy-inits a single Argon2id PHC string at first unknown-user login and caches it for the lifetime of the process. The *verification* still runs every call (the work is the security property), but we no longer pay for re-hashing on every attempt; reduces allocator pressure during auth-storms. (b) New `matchAgainstDummyKey` runs the same `ssh_pki_import_pubkey_base64` + `ssh_key_cmp` work that `matchesAnyConfiguredKey` does for a real configured key, but against a hardcoded dummy ed25519 pubkey. Called when `cfg.findUser(username)` returns null AND when a known user has `keys.len == 0`. PLAN §8.4: "failed authentication does not reveal whether the username exists" — without this, an attacker can probe valid usernames by measuring response time. (c) `failed_attempts` counter in `authenticate`, capped at 6 (matches OpenSSH `MaxAuthTries` default). On overage, emits an `auth.too_many_attempts` audit line and disconnects. Pubkey "offered" probes do NOT count (they `continue` the loop without falling through). Regression: `test/cases/21-auth-hardening.sh` (paramiko-based 7-attempt loop verifies disconnect at 6; standard sftp client offering a fresh ed25519 key under an unknown username verifies the audit line + dummy-key path is exercised).
- [x] **STAT/LSTAT are FD-based; oversize packets reply BAD_MESSAGE first** (`session.zig` `handleStat` rewrite + `readPacketTimed` graceful path). Two SFTP follow-ups in one commit. (a) `handleStat` no longer hands a path-string to `std.Io.Dir.cwd().statFile(real)`. It now resolves the parent via `openVerifiedParent` (jail-verified FD) and stats the basename through that parent FD with `follow_symlinks = false`. PLAN §7.6: STAT and LSTAT behave identically — Zift does not expose dangling-symlink semantics distinct from stat — so the symlink itself's metadata is what's returned, and the kernel never crosses the jail boundary to read the target. The previous path-string + path-based statFile approach had a TOCTOU window where a swap-with-symlink between `realpath` and `statFile` could leak outside-jail metadata; the FD-relative approach has no such window. Special-cased the root path (`/`) since it has no parent inside the jail. (b) `readPacketTimed` no longer just returns `error.LibsshFailure` on oversize frames. It now reads 5 more bytes (msg_type + request_id) so it can reply `SSH_FX_BAD_MESSAGE` for that specific request, then disconnects (we can't resync because the client said how many bytes follow and we declined to read them). Clients see a structured rejection in their logs rather than a TCP RST. Regression: `test/cases/20-stat-fd-based.sh` (oracle = an outside-jail file with distinctive 67-byte size, sabotage-tested: `follow_symlinks=true` → leaks 67, `follow_symlinks=false` → returns the symlink's own 81-byte target-path-length, no leak).
- [x] **Config and path limits enforced** (`config.zig` + `vfs.zig` + `session.zig` early gate). Closes four P1 bullets in one commit. (a) Username max 64 B (`config.zig:max_username_bytes`) — rejected at parse time with `error.UsernameTooLong`; tested at the 64/65-byte boundary. (b) Public-key line max 8192 B (`config.zig:max_keyline_bytes`) — rejected before tokenizing so an attacker-supplied huge key value cannot drive per-token allocations. (c) Virtual path max 4096 B (`vfs.zig:max_virtual_path_bytes`) and §8.3 byte-set: NUL, all C0 controls (0x01–0x1F), DEL (0x7F), and invalid UTF-8 all rejected via `std.unicode.utf8ValidateSlice`. (d) New `Vfs.validateVirtualPath` and `SftpState.ensureValidPath` early gate at every handler that takes a client path: malformed paths reply `SSH_FX_BAD_MESSAGE` *before* policy or audit run, so audit-log JSON cannot be corrupted by invalid UTF-8 in user-supplied paths and clients see the right protocol-malformed status. Sanity check: a well-formed-but-missing path still returns `NO_SUCH_FILE`, not `BAD_MESSAGE`. Regression: `test/cases/19-config-limits.sh` (3 config-side scenarios + 9 raw-SFTP path scenarios + sanity discriminator).
- [x] **SFTP wire surface matches PLAN §7.6** (`session.zig` dispatch + new `handleFstat` + 256 KiB packet buffer). Three concerns in one change. (a) Explicit cases for `SSH_FXP_SETSTAT`, `FSETSTAT`, `READLINK`, `SYMLINK`, `EXTENDED` reply `SSH_FX_OP_UNSUPPORTED` instead of the previous catch-all `SSH_FX_FAILURE`. The default arm (truly unknown opcode) also replies `OP_UNSUPPORTED` per PLAN — the session continues, no disconnect. Clients (rsync, `scp -p`, paramiko's chmod/symlink/readlink) now see "this op isn't supported" rather than "I tried and broke", which lets them fall back gracefully. (b) `SSH_FXP_FSTAT` implemented via `handle.file.?.stat()`. PLAN §7.6: "Inherits the open's permission" — no policy check at FSTAT time because OPEN already gated `.open_read`/`.open_write`. (c) SFTP packet buffer bumped from 8 KiB stack array to 256 KiB heap allocation (one per session, freed at session exit). PLAN §7.6 limit. Frames declaring length > 256 KiB are rejected as malformed and the session is torn down. Regression: `test/cases/18-sftp-wire-surface.sh` (paramiko-based raw-protocol probe with six scenarios: setstat/readlink/symlink/extended/unknown all reply OP_UNSUPPORTED; fstat returns valid attrs).
- [x] **Audit emits AFTER reply** (PLAN §8.5). Every SftpState handler now does `defer self.auditXxx(...); return replyStatus(...);` so the client gets its status reply on the wire before the audit syscall runs. A hung audit destination (file on a stalled NFS mount, FIFO with no reader, etc.) cannot delay the client's reply — it can only delay the next audit line for that worker thread. The change is a 1-line addition per call site (~30 sites across `handleStat`, `handleOpendir`, `handleOpen`, `handleRead`/`Write`, `handleMkdir`/`Remove`/`Rmdir`/`Rename`); no functional behavior visible to clients in the happy path, but the timing contract from PLAN now holds.
- [x] **Audit pipeline overhaul** (`audit.zig` rewrite + `session.zig` peer-IP capture + `main.zig` wiring + `config.zig` parser strictness). Single coherent change closes five P1 bullets. (a) `LogTarget.file` actually writes to a file: `Sink.initFromConfig` opens the path with `O_WRONLY|O_APPEND|O_CREAT|O_CLOEXEC` and stores the fd; the global singleton is initialized in `serve` after `validateSemantic`. (b) `SIGUSR1` reopen consumer: `Sink.maybeReopen` runs before every write, swaps in a fresh fd, closes the old one — full logrotate "rename old, signal, recreate" cycle works. (c) `ip` field always present: peer IP captured in `handleSession` via `getpeername` on `ssh_get_fd(session)`, threaded through `authenticate`/`handlePublicKeyMessage`/`runSftp`/`SftpState`, plus a fresh capture for the accept-loop max-connections denial. PLAN §7.4 stable order honored: `event, user, operation, result, path, detail, ip, [truncated]`. (d) Truncation: 4-tier predictive fallback (full → detail-clipped → no-path → minimal-with-ip) so over-budget lines preserve required PLAN fields and emit `"truncated":true`; never silently drops content. (e) Single-syscall writes: `std.c.write(fd, ptr, len)` for both stderr (`fd=2`) and file destinations, no `writeStreamingAll`. Rejects relative `log` paths at parse time (PLAN §7.4 absolute-only). Regressions: `15-audit-ip-field`, `16-audit-file-target`, `17-audit-sigusr1-reopen` (3 new cases, all green).
- [x] **Forced-close on grace expiration is server-driven** (`signals.{registerSessionFd,unregisterSessionFd,forceCloseAll}`). Workers register their TCP socket FD on entry and unregister on exit. When the SIGTERM/SIGINT drain wall expires, the accept thread iterates the registry and calls `shutdown(fd, SHUT_RDWR)` on every still-live session. The worker's blocked libssh read returns, the deferred cleanup chain runs (audit line, `active_sessions` decrement, ref release), and the process exits zero — the contract PLAN §7.1 makes, instead of relying on kernel-reap-on-process-exit. New `shutdown-grace` server-config knob (default 30 s, also exposed for fast integration tests). Regression: `test/cases/14-forced-close.sh` (sabotage-tested for discrimination — sabotaged code FAILs, real code PASSes).
- [x] **`zift validate` does full semantic validation** (`config.validateSemantic`). One pass that runs against the live filesystem and is shared by `zift validate`, `serve` startup, and the runtime reload path: rejects an unreadable `host-key`, rejects any user `root` that's missing or not a directory, and rejects two users whose canonicalized roots are equal or path-component-prefixes of each other. Roots are canonicalized via `realPathFileAbsoluteAlloc` so `vfs.isInsideRoot` (now `pub`) is reused for the overlap check, matching the same semantics the per-request jail uses. Diagnostics name the offending user(s) and path(s). PLAN §6.2. Closes the "validate is parse-only", "roots not canonicalized at config load", and "overlapping roots not rejected" P0s. Regression: `test/cases/13-validate-semantic.sh` (seven sub-scenarios — happy path, missing root, root-is-a-file, unreadable host-key, prefix overlap, equal roots, plus a check that `serve` refuses to start with a missing root).
- [x] **Pre-auth idle timeout enforced** (`session.zig` `setSessionTimeout`). `SSH_OPTIONS_TIMEOUT` and `SSH_OPTIONS_TIMEOUT_USEC` set on each session before `ssh_handle_key_exchange`, so libssh's blocking reads during banner exchange / KEX / USERAUTH / subsystem-open all respect the configured idle-timeout. `idle_timeout_ms == 0` disables the cap (PLAN §6.2). A new `handshake.failed` audit line gives operators visibility into pre-auth slot churn. This single change closes the "no idle timeout pre-auth" P0 *and* the "max-connections doesn't bound pre-auth" P0: stuck pre-auth slots are now reaped within the timeout window, so the slot count remains a meaningful cap under attack. Regression: `test/cases/12-pre-auth-idle.sh` (`max-connections=1`, stuck Python TCP client, oracle = does a legit `sftp` client land within `idle-timeout + slack`).
- [x] **`SSH_FXF_APPEND` honored + audit write-failure rate-limited** (`session.zig` `Handle.is_append` + `handleWrite` stat+pwrite, `audit.zig` `warnWriteFailure` + `last_warn_ms`). Two small items batched. (a) `handleOpen` parses the APPEND bit from SFTP flags, stores `is_append` on the handle. `handleWrite` detects it and computes the write offset from `file.stat().size` instead of the client-supplied offset, so SSH_FXF_APPEND clients (rsync over sftp) get the standard "all writes go to the end" semantics regardless of what offset they supplied. PLAN §7.6. (b) `audit.warnWriteFailure` now rate-limits to at most one stderr line per `warn_min_interval_ms = 5000` (5 seconds) via a `last_warn_ms` atomic with monotonic-clock comparison — a runaway audit destination cannot itself cause a stderr storm. Regression: `test/cases/24-sftp-append.sh` (raw-protocol probe sends WRITE with offset=0 against a SSH_FXF_APPEND handle; sabotage-tested for discrimination — without the is_append branch the test FAILS because the offset-0 write clobbers the existing content).
- [x] **Public-key blob validated at config load + PHC strengthened** (`config.zig` `parseUserKey` + `checkArgon2idPolicy` + `isValidBase64` helper). Two parser-side hardenings in one commit. (a) `parseUserKey` now requires the blob field to be valid OpenSSH-style base64 (`[A-Za-z0-9+/]` plus optional trailing `=` padding) — config-with-malformed-key never goes live. PLAN §8.4. (b) `checkArgon2idPolicy` now validates `v=19` literally, requires non-empty salt and hash fields, base64-validates both, and rejects any extra `$` segments after the hash. Prior parser was hand-rolled and partial; this closes the gap noted in the audit.
- [x] **Hostile integration tests added** (`test/cases/25-29`). Five new scenarios pinning behavior against malicious clients and operational pressure: 25 = auth-enumeration timing parity (known-bad-password vs unknown-user both produce `denied`); 26 = unauth-DoS (raw TCP that never speaks SSH gets reaped within idle-timeout, freeing slots for real clients); 27 = reload-stress (5 rapid SIGHUPs while a session is mid-transfer don't drop the in-flight work); 28 = log-sink-failure (audit destination becoming unwritable does not fail-close SFTP traffic); 29 = readdir edge cases (empty dir, deeply nested, hidden files, files with spaces, denied dir). Also fixes a latent bug in `test/lib/common.sh:sftp_password` where mixed single/double-quoted line continuations corrupted the expect script — never hit before because no other test exercised the helper.
- [x] **Fuzz harnesses for parser/normalizer/matcher** (`src/fuzz.zig` + `tests.zig` import). Five `std.testing.fuzz` harnesses covering the config parser, virtual path normalization (`vfs.zig`), policy glob matching, Argon2id PHC validation (driven through the parser), and public-key line validation (also driven through the parser). Each carries a corpus of known-tricky inputs so the search starts from interesting territory. Compiles into the regular test build (each harness runs once with empty input when `--fuzz` is off); run interactively with `zig build test --fuzz`. PLAN §12.
- [x] **CI workflow added** (`.github/workflows/ci.yml`). Four jobs on Ubuntu: unit tests, ReleaseSafe build with `ldd` check + smoke test + binary artifact upload, integration test suite (downloads the artifact, installs `expect` + `paramiko` + `libssh-4` runtime, runs `test/run.sh`), and a 60-second fuzz run with `--fuzz`. Uses `mlugg/setup-zig@v2` pinned to Zig `0.16.0` and apt's `libssh-dev` for build, `libssh-4` for runtime.
- [x] **`build.zig` Homebrew paths gated to macOS** (`build.zig` `is_macos` flag). The hardcoded `/opt/homebrew/{include,lib}` paths and RPATH are now only added on `target.result.os.tag == .macos`; Linux uses the default toolchain search paths so CI and Linux release builds pick up the distro `libssh-dev`. Doesn't fully close the §13 release-target story (no statically-linked release pipeline yet) but unblocks Linux CI without a `-Dbrew=true` flag.
- [x] **Deployment artifacts** (`deploy/zift.service`, `deploy/fail2ban-zift.conf`, `deploy/DEPLOY.md`). Operator-facing scaffolding: a hardened systemd unit (`NoNewPrivileges`, `ProtectSystem=strict`, `MemoryDenyWriteExecute`, `LimitNOFILE=4096`, `MemoryMax=4G`, etc.), a fail2ban filter that bans on `"result":"denied"` audit lines, and a runbook covering binary install, service-user creation, host-key generation, ZFS partner roots, audit-log layout, GCP firewall + UFW, logrotate (uses `SIGUSR1`), and rollback.
- [x] **Module-by-module security audit** (`SECURITY-AUDIT.md`). OK / CAVEAT / FINDING categorization across `session.zig` (accept loop, SSH handshake, SFTP parsing, dispatch, handles), `vfs.zig`, `config.zig`, `auth.zig`, `audit.zig`, `signals.zig`, `policy.zig`, plus a "remote attack surface summary" listing every code path between TCP accept and a privileged syscall. Used to drive the recent parser-hardening, hostile-test, and CI work.
- [x] **Independent pre-auth cap** (`config.server.max_unauth_connections`, `signals.unauth_sessions`, `session.zig` accept loop + `sessionThread` + `handleSession`). New atomic counter tracks pre-auth sessions separately from the global `active_sessions`. The accept loop checks each cap independently; when `max-unauth-connections` is configured (>0) the next pre-auth connection past the cap gets rejected with `accept.rejected` `detail="max-unauth-connections reached"` even though the global pool has headroom — a handshake-storm can't DoS authenticated partner traffic anymore (PLAN §8.4). Increment at accept; decrement at successful auth (slot freed for the next pre-auth attempt) OR at session exit if auth never completed; flag-guarded against double-decrement. `validateSemantic` rejects configurations where the unauth cap exceeds the total cap. Default is 0 (no separate cap, preserves existing behavior). `example.zift` and `deploy/DEPLOY.md` ship with `max-connections / 4` as the suggested starting point. Regression: `test/cases/30-unauth-cap.sh` (sabotage-discriminating: a no-op implementation fails the first grep; a mis-attributing implementation fails the second).
- [x] **Sftp-helper safety + systemd hardening + CI ldd assertion** (`test/lib/common.sh`, `deploy/zift.service`, `.github/workflows/ci.yml`). Three minor follow-ups closed in one commit. (a) `sftp_password` now passes the password through `ZIFT_TEST_PASSWORD` env var rather than interpolating directly into the expect script, so values containing `"`, `$`, `[`, `]`, or `\` are data not script. (b) Systemd unit gains `SystemCallFilter=@system-service ~@privileged ~@resources`, `SystemCallArchitectures=native`, `RestrictAddressFamilies=AF_INET AF_INET6`, `PrivateDevices`, `ProtectClock`, `ProtectHostname`, `ProtectProc=invisible`, `ProcSubset=pid`, `KeyringMode=private`, and an empty `CapabilityBoundingSet`/`AmbientCapabilities`. Each directive is additive and well-documented; the service still runs as `zift:zift` with the same filesystem layout. (c) CI's `ldd` step is now an assertion against an allowlist (`libssh.so.4`, `libcrypto.so.3`, `libc.so.6`, `libssl.so.3`, glibc compatibility shims) rather than informational output — anything outside the set fails the build.
- [x] **PLAN §5 allocator violation in pubkey auth closed** (`session.zig` `matchesAnyConfiguredKey` + `matchAgainstDummyKey` + `handlePublicKeyMessage`). The two pubkey-import paths used `std.heap.page_allocator` for per-attempt scratch dupes — a global allocator forbidden by PLAN §5 ("no global allocator"). Threaded the session's allocator down through `handlePublicKeyMessage` to both functions. Symmetry preserved: the configured-key and dummy-key paths still use the SAME allocator, so the timing-parity property PLAN §8.4 commits to (unknown-user attempts indistinguishable from known-user-but-wrong-key attempts) is unchanged. Also reframed the corresponding entry in `SECURITY-AUDIT.md`: the spec violation is now closed; the optional follow-up (per-session caching of parsed `ssh_key` handles to skip the repeat decode) is documented as a CAVEAT with the explicit constraint that EITHER both paths get the cache OR neither, never just one.
- [x] **`zig build release` foundation** (`build.zig` `release` step + ReleaseSafe-tagged `build_options`). Produces `zig-out/release/zift-{version}-{target}` plus a `SHA256SUMS` line, both regenerated on every invocation. The release `build_options` always reports `optimize = "ReleaseSafe"` so `zift version` on a production host shows the correct optimization level regardless of any `-Doptimize=...` the dev passed for the default target. Cross-target via `-Dtarget=aarch64-linux-gnu` etc. WORKS as far as the `release` step is concerned — what doesn't work yet is the actual cross-link, because libssh isn't vendored. Once the static-linking work lands, the same invocation will produce a static binary for any of the four PLAN §4.7 targets without further changes to this step. PLAN §13 deliverables: foundation + manifest landed; signing + static linking + release CI workflow tracked as the remaining P1.
- [x] **Release-artifact dependency-surface verification** (`build/verify-release.sh` + `release` step dependency). The script dispatches on ELF vs Mach-O magic bytes (so it works on any host inspecting any target binary), extracts the runtime dependency list (`objdump -p NEEDED` for ELF, `otool -L` for Mach-O), and matches each entry against an allowlist. Today's allowlist accepts the dynamic libssh + libc + Mach-O libSystem entries the build legitimately produces; phase-2 (after static linking lands) flips the Linux allowlist to `^$` so the literal "zero NEEDED" PLAN §13 promise holds. Sabotage-verified: with phase-2's stricter allowlist, today's binary is correctly REJECTED for carrying a Homebrew libssh path. Wired into `zig build release` so a regression in the release dependency surface fails the build, not the deploy. Same script will be reused by the future release CI workflow.
- [x] **Release CI workflow + signing** (`.github/workflows/release.yml` + `build.zig` `-Dversion=` flag + `deploy/DEPLOY.md` verification section). Triggered on `vX.Y.Z` (or `vX.Y.Z-prerelease`) tag push. Builds release artifacts on `ubuntu-latest` (Linux x86_64) and `macos-latest` (macOS aarch64) in parallel, then aggregates them in a publish job that: (a) builds a unified `SHA256SUMS` covering every target's binary; (b) signs it via cosign keyless using GitHub's OIDC token, producing a `SHA256SUMS.bundle` whose embedded certificate proves "this signature was made by THIS workflow run on shreeve/zift" (no long-lived secrets, no GPG key management); (c) creates a GitHub Release with all binaries + SHA256SUMS + SHA256SUMS.bundle attached; (d) auto-flags `vX.Y.Z-rc.N`-style tags as prereleases. `build.zig` learned a `-Dversion=` build option so the workflow extracts the version from the tag (`v0.1.0` → `0.1.0`) and passes it through; local `zig build release` keeps the source-tree default. `deploy/DEPLOY.md` got a "Verify provenance + integrity" section that gives partners the exact `cosign verify-blob` + `shasum -a 256 -c` commands. Coverage gaps: Linux aarch64 + macOS x86_64 still need cross-build via vendored libssh (the remaining static-linking P1).

---

## P0 — Blockers

These break a security promise PLAN.md makes by name. None of them is shippable as-is.

_(no remaining P0 entries — all closed)_

---

## P1 — Major

PLAN.md promises that the code does not yet keep, but no immediate security regression.

### Audit pipeline

_(Audit pipeline overhaul landed: see DONE entries below. Write-failure rate-limiting landed via `last_warn_ms` atomic in `audit.zig`.)_

### Config parser & validator

_(Limits, path validation, public-key blob base64 check, and PHC strengthening landed: see DONE entries below.)_
_(Reload-semantics cluster landed: see DONE entry below.)_

### SFTP protocol surface

_(Wire-surface protocol items landed: see DONE entries below.)_

### Authentication

- [x] **`pk_ok` reveals whether (user, key) is configured.** Resolved as option (a): this is an inherent SSH protocol property. The two-phase pubkey flow (`SSH2_MSG_USERAUTH_PK_OK` → signed follow-up) reveals whether a key is accepted *before* the client signs. OpenSSH itself has the same property. Documented as an explicit caveat: password auth uses timing-safe dummy verification for unknown users (PLAN §8.4), but pubkey probing is an SSH-level information channel that cannot be closed without breaking the protocol. Operators who need to mask user existence should use password-only auth or a jump host.
_(Public-key matching `page_allocator` violation closed: see DONE entry below. The remaining piece — caching parsed `ssh_key` handles to avoid the repeat decode — is now an OPTIONAL optimization, not a spec violation, and is tracked as a CAVEAT in `SECURITY-AUDIT.md` rather than a P1 here.)_

### Operational

_(Separate pre-auth cap landed: see DONE entry below.)_

### Build & release

_(Homebrew paths gated to macOS, fuzz targets added, CI workflow added: see DONE entries below.)_

- [ ] **Release target: only static linking remains.** Signing + release workflow + verification infrastructure all landed (see DONE entries below). The single remaining piece is static linking: vendor libssh + libcrypto sources via `build.zig.zon` so (a) cross-target builds don't need a sysroot, (b) the published Linux binary runs on any host without `apt install libssh-4`, (c) `verify-release.sh`'s `LINUX_ALLOWED` allowlist tightens to `^$` (the literal "zero NEEDED" check). Once libssh is vendored, the same `zig build release -Dtarget=...` invocation produces a portable static binary for any of the four PLAN §4.7 targets without further changes to the release step or the workflow.
_(Static-link verification infrastructure landed: see DONE entry below. The script lives at `build/verify-release.sh` and runs on every `zig build release`. Today it asserts the small allowlist of known dynamic deps; phase-2 (after static linking lands) flips the Linux allowlist to `^$` so the literal "zero NEEDED" PLAN §13 promise holds.)_

---

## P2 — Minor / Polish

- [ ] **`ConfigRef` lifetime is correct but untested under load.** PLAN §7.3 mandates that an existing session keeps its old config alive across reloads. Add a stress test: 32 concurrent sessions, hammer config reloads on a separate thread, observe no use-after-free / double-free.
- [ ] **`active_sessions` lives in `signals.zig`.** It isn't signal state. Move to a `server_state.zig` or attach to `ActiveConfig`.
- [ ] **`channel` cleanup on session exit.** `handleSession` doesn't explicitly `ssh_channel_send_eof` / `ssh_channel_close` / `ssh_channel_free`. We removed an explicit close earlier because of a segfault, but the right fix is to call EOF + close + free in a single defer, in the right order.

---

## Disputed / Won't fix

Items raised by peer review (GPT-5.5) that we deliberately do not accept,
plus design choices we deliberately make. Documented here so a future
reviewer doesn't re-litigate ground we've already walked.

- **Audit every READ/WRITE chunk** — design choice (PLAN §8.5). OPEN
  audits the privilege grant; subsequent read/write within an authorized
  handle is not separately audited. Auditing every chunk would log-storm
  normal transfers, drown the actual privilege events, and force operators
  to write filters to recover signal. The chunk-level data is recoverable
  from accounting (bytes per session) without per-chunk JSON. _GPT-5.5
  disputed; we keep the decision._

- **Audit every REALPATH / STAT / LSTAT / READDIR** — same reasoning as
  above. These ops don't mutate state and don't expose content beyond
  metadata the client already has authority to see (read+list policy
  permission). If a deployment finds enumeration probes worth recording,
  the right intervention is auditing the FAILURE path (denied stat in a
  policy-restricted area), not the success path. _GPT-5.5 disputed; we
  keep the decision._

- **`SA_RESTART` on operational signal handlers** — deliberate choice.
  The accept loop polls with 1-second slices, so signal latency is bounded
  even when SA_RESTART blocks the signal from interrupting blocked
  syscalls. The trade we don't want is signals interrupting `read(2)` /
  `write(2)` calls inside libssh worker threads, where libssh's error
  recovery is harder to reason about than just letting the next poll cycle
  pick up the flag. _GPT-5.5 flagged this; we keep the choice and document
  it here._

- **`std.posix.sigaction` "return value ignored"** — non-issue. In Zig
  0.16 the function returns `void` and is `unreachable` on EINVAL. There
  is no return value to check. _GPT-5.5 misread the API; no action
  needed._
